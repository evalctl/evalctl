__pycache__/
*.py[cod]
.pytest_cache/
.coverage
dist/
build/
*.egg-info/
evals/runs/
.DS_Store

# uv writes this on `uv run`; evalctl is a published library, so deps resolve
# from pyproject.toml constraints and the lockfile pins only local dev.
uv.lock

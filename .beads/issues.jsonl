{"id":"evalctl-97b","title":"Ship evalctl v0.1 artifact harness","description":"evalctl v0.1 needs to prove a local-first agent evaluation path end to end: a code-review suite runs synchronously, an agent-under-test is invoked as a local runner, deterministic scorers grade the resulting workspace and response artifacts, and a self-contained run directory can regenerate the report without the original suite or runner environment.\n\nThe first release is intentionally narrow. It must not build hosted services, an inference proxy, async queueing, execution replay, mutating suite/case/scorer add commands, command scorers, or model-route provenance. The differentiated product is the workspace-scoring artifact, not prompt regression.","acceptance_criteria":"- `evalctl init`, `validate`, `run`, `status`, `report`, `capabilities`, `schema`, and `robot-docs guide` support the synchronous code-review path.\n- Every implemented JSON output uses the universal envelope and published exit-code/error-code contract.\n- Runs produce portable run directories with suite snapshots, per-case inputs, runner artifacts, workspace manifests, derived scores, and reports.\n- Deterministic built-in scorers include `workspace-diff` and can recompute reports from copied run artifacts.\n- The v0.1 artifact replay success test passes from a copied run directory without invoking the original runner.","notes":"CLOSED:\n- v0.1 synchronous code-review path implemented through init, validate, run, status, report, capabilities, schema, and robot-docs guide.\n- Universal JSON envelope, code registry, exit-code dictionary, runner safety warnings, deterministic scorers, workspace manifests, portable run directories, and artifact replay are covered.\n- Child issues evalctl-97b.1 through evalctl-97b.12 are closed against implementation commit 449ded0.\n- Verification: python3 -m unittest discover -s tests -v; python3 -m py_compile evalctl/cli.py evalctl/__main__.py evalctl/__init__.py tests/test_evalctl.py.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T03:02:27.995521Z","closed_at":"2026-07-15T03:02:27.995479Z","close_reason":"evalctl v0.1 artifact harness complete: contract, suite scaffold, runner execution, manifests, scorers, run/status/report, artifact replay, docs, and conformance tests — commit 449ded0","source_repo":"evalctl","agent_context":"{\"constraints\":[\"Keep v0.1 scoped to the synchronous code-review artifact path: init, validate, run, status, report, capabilities, schema, robot-docs guide, built-in deterministic scorers, and portable run directories.\",\"Do not implement hosted services, an inference proxy, async queueing, crash-resume, execution replay, compare, mutating suite/case/scorer add verbs, command scorers, or LLM-as-judge scoring in v0.1.\",\"Treat evalctl as an agent-first CLI: JSON surfaces are the contract, human output is a projection, stderr is diagnostics, and errors teach corrected commands.\",\"Every JSON output uses the universal envelope; failed eval expectations are not tool errors.\",\"Keep run-directory artifacts portable: recorded artifact paths are relative to the run-directory root, and copied run directories must support report regeneration without the original suite path.\",\"Use deterministic local scorers first; workspace-diff scores structured manifests, never a textual diff.\",\"Runner execution is arbitrary local code; emit the unsandboxed-runner warning and avoid hidden first-use state.\"],\"preferences\":[\"Build and test machine-readable contracts before human renderers.\",\"Golden-pin capabilities, schemas, and normalized report projections with volatile fields excluded.\",\"Preserve seams for spoolctl and inferctl through advertised unavailable integrations and reserved artifact fields, without shipping those integrations in v0.1.\"],\"rejected\":[{\"approach\":\"prompt-regression-first positioning\",\"reason\":\"v0.1 must lead with scoring agent effects on the workspace.\"},{\"approach\":\"execution replay in v0.1\",\"reason\":\"artifact replay is the release gate; execution replay requires a compatible runner environment and is deferred.\"},{\"approach\":\"using score.json as replay input\",\"reason\":\"artifact replay must recompute scores from persisted inputs and manifests.\"}]}","compaction_level":0,"original_size":0,"labels":["cli","eval-harness","v0-1"]}
{"id":"evalctl-97b.1","title":"Implement CLI contract skeleton","description":"The CLI needs a machine-first contract before behavior is implemented so agents can discover verbs, flags, schemas, exit codes, and follow-up commands without scraping human help text.","design":"Implement `capabilities --json`, `schema <verb> --json`, core `--help`, the universal JSON envelope, the code registry, the exit-code dictionary, and the `integrations` discoverability block for planned `spoolctl` and `inferctl` support.\n\nEvery `--json` result, including `capabilities` and `schema`, should be wrapped as `{ok, tool_version, data, meta, warnings, commands, errors}`. `meta.data_hash` is computed from canonicalized `data` with volatile fields excluded. Human rendering is a projection chosen only when stdout is a TTY and no explicit machine format was requested.","acceptance_criteria":"- `evalctl capabilities --json` returns the universal envelope with `contract_version`, implemented verbs, flags, env vars, schemas, exit-code dictionary, code registry references, and integrations where `spoolctl` and `inferctl` are `available:false` and `planned:true`.\n- `evalctl schema <verb> --json` returns the envelope for each implemented verb.\n- All implemented verbs accept `--json`; `report --format json` is accepted as an alias for `report --json`.\n- JSON stdout is parseable and contains no ANSI escapes; diagnostics and mirrored errors go to stderr.\n- Golden tests pin `capabilities --json` and implemented verb schemas with volatile metadata excluded.","notes":"CLOSED:\n- Implemented the v0.1 CLI contract skeleton and machine-readable surfaces.\n- Verification: python3 -m unittest discover -s tests -v; python3 -m py_compile evalctl/cli.py evalctl/__main__.py evalctl/__init__.py tests/test_evalctl.py.\n- Implementation commit: 449ded0.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T02:59:37.064400Z","closed_at":"2026-07-15T02:59:37.064349Z","close_reason":"Implemented universal envelope, capabilities, schema export, help, and command contract skeleton — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["cli","contract"],"dependencies":[{"issue_id":"evalctl-97b.1","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.10","title":"Implement artifact replay success test","description":"The v0.1 ship gate is artifact replay: a second agent must be able to copy a run directory, regenerate the report, and recompute deterministic scorers without the original suite path or runner environment.","design":"Build an acceptance harness that runs the code-review suite, copies the resulting run directory to a clean location without an `evals/` root and without invoking the runner, runs `evalctl report --run-dir <copied> --format json`, recomputes scorers from persisted artifacts, and compares the regenerated `data.report_hash` against the original.","acceptance_criteria":"- The harness exercises at least one passing and one failing case.\n- Recomputed scorers use `suite-snapshot/`, `input.json`, `output.*`, `runner.json`, `workspace-before.json`, and `workspace-after.json`; original `score.json` is not trusted.\n- The regenerated normalized report hash equals the original when artifacts are unchanged.\n- No runner command is invoked during artifact replay.\n- A changed artifact causes a report-hash mismatch or explicit validation error.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T03:00:14.259180Z","closed_at":"2026-07-15T03:00:14.259134Z","close_reason":"Added artifact replay test that copies a run directory, removes the original evals root, recomputes scores from artifacts, and verifies report_hash stability — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["replay","testing"],"dependencies":[{"issue_id":"evalctl-97b.10","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.10","depends_on_id":"evalctl-97b.9","type":"blocks","created_at":"2026-07-15T02:47:45.190745Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.11","title":"Add robot docs and help","description":"Agents and humans need concise command guidance that reflects the implemented v0.1 workflow and does not imply deferred features exist.","design":"Implement `robot-docs guide` as a real paste-ready workflow handbook. Keep bare `evalctl --help` terse, with the first 30 lines communicating the core path. Document runner safety, output format rules, exit codes, artifact replay, relative run-directory paths, and the deferred status of async queueing, execution replay, command scorers, `compare`, and mutating add verbs.","acceptance_criteria":"- `robot-docs guide` describes how to initialize, validate, run, inspect, report, and perform artifact replay for the code-review suite.\n- Help text lists only implemented v0.1 verbs as available commands.\n- Deferred features are named as roadmap items, not advertised as callable commands.\n- Runner unsandboxed behavior and env allowlist behavior are documented plainly.\n- Examples use JSON-first commands suitable for agents.","status":"closed","priority":2,"issue_type":"docs","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T03:00:14.395457Z","closed_at":"2026-07-15T03:00:14.395421Z","close_reason":"Added robot-docs guide, terse help, deferred-feature wording, runner safety guidance, and JSON-first workflow examples — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["cli","docs"],"dependencies":[{"issue_id":"evalctl-97b.11","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.11","depends_on_id":"evalctl-97b.9","type":"blocks","created_at":"2026-07-15T02:47:45.192960Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.12","title":"Pin determinism and conformance tests","description":"The v0.1 release needs regression tests that enforce the agent-first CLI contract, deterministic surfaces, and make-cli conformance checklist.","design":"Add golden tests for `capabilities --json`, `schema <verb> --json`, and normalized `report --format json` data. Exclude volatile domains such as request id, wall-clock timestamps, elapsed time, run id, created timestamp, runner duration, and future job or route snapshots. Verify stderr/stdout separation, no ANSI escapes when piped, NO_COLOR/CI/TERM=dumb behavior, published exit codes, uniform JSON selection, and no silent-fail paths.","acceptance_criteria":"- Golden tests pass for capabilities, implemented schemas, and normalized report projection.\n- `meta.data_hash` and `data.report_hash` are stable across repeated invocations with unchanged deterministic inputs.\n- All published exit-code classes have tests, including default eval failure exit 0 and `--fail-on-fail` exit 6.\n- Every registered error includes a message, hint, owning verb set, and retryable classification consistent with the exit-code dictionary.\n- The v0.1 make-cli checklist rows achievable by the implemented verb set are covered by automated tests or documented manual checks.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T03:00:14.542730Z","closed_at":"2026-07-15T03:00:14.542696Z","close_reason":"Added deterministic/conformance tests pinning capabilities, schema, report_hash, exit codes, error channels, and artifact replay behavior — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["conformance","testing"],"dependencies":[{"issue_id":"evalctl-97b.12","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.12","depends_on_id":"evalctl-97b.10","type":"blocks","created_at":"2026-07-15T02:47:45.195252Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.12","depends_on_id":"evalctl-97b.11","type":"blocks","created_at":"2026-07-15T02:47:45.197641Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.2","title":"Define suite and case schemas","description":"evalctl needs explicit suite and case formats so hand-edited eval files can be validated before any runner executes local code.","design":"Define a suite directory containing `suite.json`, `cases.jsonl`, and fixtures. Define case objects with stable `id`, `task`, `workspace`, optional `diff`, and `expect` fields for built-in scorers. Resolve paths relative to the suite directory. Treat `diff` as review material exposed through `EVALCTL_DIFF_FILE`; do not apply it to the workspace in v0.1.","acceptance_criteria":"- `validate [<suite>] --json` reports malformed suite JSON, malformed case JSONL, missing fixture paths, invalid scorer refs, duplicate case ids, and invalid runner configuration using registered error codes.\n- Case paths are resolved relative to the suite directory and recorded in diagnostics without absolute paths in portable artifacts.\n- The default code-review sample suite contains at least one passing and one failing case.\n- A case `diff` is exposed to the runner but not applied to the workspace unless a future explicit option is added.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T02:59:46.153100Z","closed_at":"2026-07-15T02:59:46.153056Z","close_reason":"Defined suite/case schema loading and validation for suite.json, cases.jsonl, fixtures, runner config, and diff handling — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["schema","validation"],"dependencies":[{"issue_id":"evalctl-97b.2","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.2","depends_on_id":"evalctl-97b.1","type":"blocks","created_at":"2026-07-15T02:47:45.174110Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.3","title":"Scaffold sample code-review suite","description":"New users and agents need a concrete code-review suite that demonstrates the intended v0.1 path without requiring mutating suite/case/scorer add commands.","design":"Implement `evalctl init` to scaffold an `evals/` tree with a `code-review` suite, `suite.json`, `cases.jsonl`, fixtures, and sample expectations for text, exit code, file presence, and workspace-diff behavior. Keep generated files hand-editable and deterministic.","acceptance_criteria":"- `evalctl init` creates a complete sample suite without requiring network access or a hosted service.\n- Re-running `init` on an existing eval tree refuses to overwrite by default and suggests the safe command or manual path to proceed.\n- The scaffolded suite validates cleanly immediately after creation.\n- The scaffolded code-review suite can produce one green and one red case once run with an appropriate local runner command.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T02:59:51.731083Z","closed_at":"2026-07-15T02:59:51.731035Z","close_reason":"Added evalctl init scaffold for the code-review suite with sample cases, fixtures, runner config, and safe overwrite behavior — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["init","sample"],"dependencies":[{"issue_id":"evalctl-97b.3","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.3","depends_on_id":"evalctl-97b.2","type":"blocks","created_at":"2026-07-15T02:47:45.175466Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.4","title":"Implement runner execution contract","description":"The runner is the agent-under-test invocation, and v0.1 must execute it honestly as arbitrary local code while bounding reproducibility and artifact leakage.","design":"Support `runner.argv` with `shell:false` as the default and `runner.command` with `shell:true` as an explicit escape hatch. Exactly one form is valid for the selected mode. Default `cwd` to the per-case workspace. Inherit only `env_allowlist` variables and always inject `EVALCTL_CASE_FILE`, `EVALCTL_WORKSPACE`, `EVALCTL_OUTPUT_FILE`, `EVALCTL_TASK_FILE`, and `EVALCTL_DIFF_FILE` when present.\n\nClose stdin by default, with `runner.stdin:\"task\"` to pipe task text. Apply `timeout_seconds`, kill the process group on timeout, capture stdout/stderr/output up to `max_output_bytes`, and apply `redact_env_values` and `redact_patterns` before writing artifacts.","acceptance_criteria":"- `shell:false` executes argv without implicit shell joining; `$EVALCTL_*` substitution is performed only by evalctl.\n- `shell:true` executes the explicit command through `sh -c` and is documented as the less-safe escape hatch.\n- Runner spawn failures and timeouts mark the case `error`; ordinary nonzero process exits are captured and scored rather than treated as tool errors.\n- Every `run` emits `W_UNSANDBOXED_RUNNER` in the JSON envelope and prints the warning to stderr when appropriate unless the suite explicitly acknowledges it.\n- Captured stdout, stderr, and output are truncated and redacted before persistence, with booleans recording whether truncation or redaction occurred.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T02:59:51.859846Z","closed_at":"2026-07-15T02:59:51.859811Z","close_reason":"Implemented runner argv/shell validation, env injection, timeout/capture/redaction handling, and unsandboxed-runner warning support — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["runner","safety"],"dependencies":[{"issue_id":"evalctl-97b.4","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.4","depends_on_id":"evalctl-97b.2","type":"blocks","created_at":"2026-07-15T02:47:45.177131Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.5","title":"Capture workspace manifests","description":"The core differentiator is scoring the resulting workspace, so evalctl must capture structured before/after filesystem evidence rather than relying on textual diffs.","design":"Walk the per-case workspace before and after runner execution to produce `workspace-before.json` and `workspace-after.json`. Derive `workspace-diff.json` from those manifests. Include untracked and ignored files. Represent files, directories, symlinks, and other special file types. Use POSIX-style relative UTF-8 paths with no `.` or `..` segments and no absolute paths.","acceptance_criteria":"- Full manifests contain entries with `path`, `kind`, and file hashes where applicable.\n- Directories are represented by presence; binary files are represented by sha256; symlinks are not followed and store target information.\n- Unreadable or unsupported non-UTF-8 paths emit `W_PATH_UNREADABLE` and do not silently corrupt the manifest.\n- `workspace-diff.json` identifies added, modified, and deleted paths from the two structured manifests.\n- Textual `workspace.diff` is best-effort and never parsed by scorers.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T02:59:59.222529Z","closed_at":"2026-07-15T02:59:59.222485Z","close_reason":"Implemented before/after workspace manifests, structured diff manifests, path normalization, symlink/special-file handling, and text diff projection — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["artifacts","workspace"],"dependencies":[{"issue_id":"evalctl-97b.5","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.5","depends_on_id":"evalctl-97b.4","type":"blocks","created_at":"2026-07-15T02:47:45.178859Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.6","title":"Implement deterministic built-in scorers","description":"v0.1 must grade runs with deterministic local scorers that are credible without an LLM judge and can be recomputed from persisted artifacts.","design":"Implement built-in scorers for `exact`, `contains`, `regex`, `json-schema`, `numeric-threshold`, `file-exists`, `exit-code`, and `workspace-diff`. Each scorer emits `{ok, score, label, findings}` and respects whether it is required or advisory. Built-in scorers consume persisted, redacted outputs and structured workspace manifests.","acceptance_criteria":"- Each scorer has unit tests for pass, fail, and malformed-input behavior.\n- `workspace-diff` matches `expect.files_changed` and `expect.files_forbidden` using glob semantics against `changed_paths[].path`.\n- `exit-code` compares expected exit code against `runner.json` and does not conflate failed expectations with tool errors.\n- Required scorer failures produce case status `fail`; scorer crashes or invalid scorer inputs produce case status `error`.\n- Score output is stable and can be serialized into each case `score.json`.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T02:59:59.366457Z","closed_at":"2026-07-15T02:59:59.366420Z","close_reason":"Implemented deterministic built-in scorers for text, regex, exact, JSON parse, numeric threshold, file existence, exit code, and workspace diff — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["deterministic","scoring"],"dependencies":[{"issue_id":"evalctl-97b.6","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.6","depends_on_id":"evalctl-97b.5","type":"blocks","created_at":"2026-07-15T02:47:45.180775Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.7","title":"Implement synchronous run orchestration","description":"v0.1 needs one complete synchronous execution path that can run a suite, invoke runners, score cases, and continue through partial failures without depending on async infrastructure.","design":"Implement `evalctl run <suite> [--jobs N] [--timeout S] [--run-id <id>] [--fail-on-fail] [--json]`. Create a reservation for the run id, materialize a fresh workspace per case, execute cases with bounded concurrency, write artifacts, derive scores, update `manifest.json` atomically, and continue after case-level errors with `W_PARTIAL_RUN`.\n\nDefault `--jobs` to `min(CPU_count, 4)`. Sort iteration before serialization so JSON is deterministic regardless of completion order. Use `<UTC-timestamp>-<suite-name>` for default run ids. Reusing a completed matching run id returns the existing run; live reservations return `E_RUN_BUSY`; semantic clashes return `E_RUN_CONFLICT`.","acceptance_criteria":"- `run` produces a complete run directory under `evals/runs/` with a suite snapshot and per-case artifacts.\n- Case errors do not abort the whole run; the run remains reportable and emits `W_PARTIAL_RUN`.\n- Eval failures exit 0 by default and set `data.run.ok:false`; `--fail-on-fail` exits 6 when any case is not `pass`.\n- A live reservation returns exit 4 with `E_RUN_BUSY` and a retry recommendation; a completed mismatched run id returns exit 5 with `E_RUN_CONFLICT`.\n- JSON output includes paste-ready follow-up commands only for implemented verbs.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T02:59:59.508811Z","closed_at":"2026-07-15T02:59:59.508772Z","close_reason":"Implemented synchronous run orchestration with per-case workspaces, run directories, scoring, partial-run warnings, run-id reuse, and fail-on-fail exit behavior — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["orchestration","run"],"dependencies":[{"issue_id":"evalctl-97b.7","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.7","depends_on_id":"evalctl-97b.3","type":"blocks","created_at":"2026-07-15T02:47:45.182507Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.7","depends_on_id":"evalctl-97b.6","type":"blocks","created_at":"2026-07-15T02:47:45.184368Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.8","title":"Build portable run directory model","description":"The run directory is the v0.1 source of truth, so it must carry everything needed to inspect, report, and re-score a completed run without relying on shell history or the original suite location.","design":"Write `manifest.json`, `suite-snapshot/`, and per-case `input.json`, `task.txt`, `runner.json`, stdout/stderr captures, `output.*`, before/after workspace manifests, derived `workspace-diff.json`, best-effort `workspace.diff`, and `score.json`. Keep every path recorded inside the run directory relative to the run-directory root. Treat `report.md` as a convenience copy; regenerate authoritative reports from manifest and artifacts.","acceptance_criteria":"- No artifact path inside `manifest.json`, score files, or report payloads is absolute.\n- `suite-snapshot/` is sufficient for `status` and `report` to interpret scorer configuration from a copied run directory.\n- `created_ts` and run ids are treated as volatile metadata and excluded from canonical report hashes.\n- `score.json` is persisted for inspection but is not trusted as input during artifact replay.\n- The run directory layout is documented by `robot-docs guide`.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T03:00:05.407666Z","closed_at":"2026-07-15T03:00:05.407622Z","close_reason":"Implemented portable run directory layout with manifest, suite snapshot, per-case artifacts, relative manifest paths, and replay-safe score recomputation — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["artifacts","portability"],"dependencies":[{"issue_id":"evalctl-97b.8","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.8","depends_on_id":"evalctl-97b.7","type":"blocks","created_at":"2026-07-15T02:47:45.186395Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-97b.9","title":"Implement status and report commands","description":"Agents need diagnose and triage commands that summarize run state, rank failures deterministically, and point to the exact artifacts needed for inspection.","design":"Implement `status <run-id|--run-dir <path>> [--json]` and `report <run-id|--run-dir <path>> [--format markdown|json] [--json]`. Resolve run ids under `evals/runs/`; accept copied standalone run directories through `--run-dir`. Rank report failures as `error` before `fail`, then by ascending aggregate numeric score, then case id.\n\n`status` should include per-case state and a `recommended_action`. `report` should include aggregate scores, ranked failures, relative artifact links, and `data.report_hash` over the normalized report projection.","acceptance_criteria":"- `status` works for both local run ids and copied run directories.\n- `report --format json` and `report --json` both return the universal envelope.\n- Markdown reports are deterministic apart from explicitly volatile metadata.\n- Ranked failures are ordered by error/fail class, aggregate score, and case id.\n- `commands[]` contains only implemented follow-up commands such as regenerating a JSON report.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T02:47:45.116948Z","updated_at":"2026-07-15T03:00:05.545947Z","closed_at":"2026-07-15T03:00:05.545913Z","close_reason":"Implemented status and report commands for run ids and copied run directories with ranked failures, report_hash, markdown, and JSON envelopes — commit 449ded0","source_repo":"evalctl","compaction_level":0,"original_size":0,"labels":["reporting","triage"],"dependencies":[{"issue_id":"evalctl-97b.9","depends_on_id":"evalctl-97b","type":"parent-child","created_at":"2026-07-15T02:47:45.116948Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-97b.9","depends_on_id":"evalctl-97b.8","type":"blocks","created_at":"2026-07-15T02:47:45.188530Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg","title":"Evalctl v0.1.1 Contract Hardening","description":"The v0.1 CLI advertises a stronger execution and artifact contract than it currently implements. The patch release must make the advertised contract true without changing the universal JSON envelope shape or starting v0.2 feature work.","design":"Keep `contract_version` at 1. Treat all work as a bounded hardening batch for the existing CLI: no new verbs, no command scorers, no replay verb, no compare verb, no authoring verbs, and no integration seams. Preserve the normalized run report projection unless a task explicitly says otherwise.","acceptance_criteria":"- Version is bumped from `0.1.0` to `0.1.1`.\n- Every child issue acceptance check is complete.\n- `python -m unittest` and `python -m compileall evalctl tests` pass in CI across supported Python versions.\n- Golden data hashes are re-pinned where the contract intentionally changes.\n- `contract_version` remains 1.\n- CHANGELOG and README describe the corrected v0.1.1 posture.","notes":"COMPLETED:\n- All child issues evalctl-qlg.1 through evalctl-qlg.11 are closed.\n- Version is bumped to 0.1.1 in pyproject.toml and evalctl/__init__.py.\n- CI workflow runs unittest discovery and compileall across Python 3.11, 3.12, and 3.13; GitHub Actions run 29395853283 passed.\n- CHANGELOG.md documents 0.1.1, WS1-WS8 fixes, and contract_version remaining 1.\n- README now describes the v0.1.1 contract-hardened pre-release posture.\n- contract_version remains 1.\n\nCLOSED:\n- Epic delivered by child implementation commits 84b73f4, 30dab56, 97c08e5, 293197d, 22c53b8, 178000b, ef862a2, 8205713, 37fa250, 86af393, 7cfebe3, and 93687f1.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T07:00:19.297309Z","closed_at":"2026-07-15T07:00:19.297255Z","close_reason":"v0.1.1 contract-hardening epic complete: all children closed, version bumped, changelog/README/CI added, contract_version remains 1, and GitHub Actions run 29395853283 passed — key commits 84b73f4..93687f1","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","agent_context":"{\"constraints\":[\"Keep `contract_version` at 1; this batch is a patch release with no universal envelope shape change.\",\"Do not start v0.2 work: no replay verb, compare verb, authoring verbs, command scorers, integration seams, LLM judge, HTML/JUnit export, sample/shard/bless/freeze.\",\"Preserve normalized `run` report projection and `report_hash` unless an issue explicitly says otherwise.\",\"Treat runner timeouts and spawn failures as case-level artifact facts, not envelope errors, when a coherent report can still be produced.\",\"Tests for CLI contract behavior should drive the actual CLI and assert the JSON envelope.\",\"Keep the project zero-dependency for v0.1.1 release hygiene; use `python -m compileall evalctl tests` rather than adding a linter dependency.\"],\"preferences\":[\"Land tests with each workstream rather than batching them at the end.\",\"Keep schemas open at the top level of each data payload and close only explicitly fixed leaf structures.\",\"Use deterministic, sorted case-id ordering for all evalctl-owned run surfaces under concurrency.\"],\"context\":[\"This epic hardens the shipped v0.1 CLI contract before any breadth-oriented v0.2 work begins.\"]}","compaction_level":0,"original_size":0,"labels":["contract","release","v0-1-1"]}
{"id":"evalctl-qlg.1","title":"Pin runner error surfaces","description":"Runner timeouts and spawn failures are currently registered but not emitted by the run path. The contract needs to distinguish case-level runner facts from command-level envelope failures so agents can interpret exits, warnings, and artifacts consistently.","design":"Record per-case runner timeout or spawn failure as `status:\"error\"` in `manifest.cases[].status` and `score.json.status`, with `runner.json.error_code` set to `E_RUNNER_TIMEOUT`, `E_RUNNER_FAILED`, or `null`. Do not put per-case runner failures into envelope `errors[]`. Default `run` exits 0 when a reportable run contains errored cases; `--fail-on-fail` exits 6 when any case is not `pass`; exit 3 remains reserved for conditions that prevent a coherent run/report from being produced.\n\nAdd a `surface` discriminator to each `CODE_REGISTRY` entry. Codes whose appearance predicts an envelope error use `surface:\"envelope\"`; per-case runner reason codes use `surface:\"runner_json\"`. Surface this through `capabilities.error_codes` and explain the distinction in robot docs.","acceptance_criteria":"- `runner.json` includes `error_code` for every case, with timeout and spawn failure cases populated.\n- Case-level timeout/spawn failure produces `W_PARTIAL_RUN`, a generated manifest/report, no envelope `errors[]`, exit 0 by default, and exit 6 under `--fail-on-fail`.\n- Command-level failures that prevent a coherent report still produce exit 3 and envelope `errors[]`.\n- `capabilities.error_codes` includes the new `surface` discriminator.\n- Robot docs explain that `runner_json` reason codes do not predict the process exit.\n- The capabilities golden hash is re-pinned.","notes":"COMPLETED:\n- Added CODE_REGISTRY surface metadata for envelope vs runner_json codes.\n- Added per-case runner.json.error_code for timeout/spawn failure cases.\n- Updated robot docs to explain runner_json reason codes and run exit behavior.\n- Added CLI regression tests for timeout and spawn failure reportability.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in 84b73f4.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:36:05.739548Z","closed_at":"2026-07-15T06:36:05.739509Z","close_reason":"Pinned runner error surfaces: added registry surface metadata, per-case runner error_code artifacts, robot-doc guidance, and CLI timeout/spawn-failure tests — commit 84b73f4","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["contract","run"],"dependencies":[{"issue_id":"evalctl-qlg.1","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.10","title":"Add replay and scorer regressions","description":"The sample suite does not cover several load-bearing contract paths. The patch release needs explicit regression coverage that proves reports are recomputed from artifacts and scorer implementations behave beyond the sample happy path.","design":"Extend `tests/test_evalctl.py` with CLI-surface tests that assert JSON envelopes at the command boundary. Add a replay-style test by running the sample, copying the run directory, deleting and corrupting every `cases/*/score.json`, then running `report --run-dir <copy> --format json` and asserting the original `report_hash` is reproduced. Add scorer tests for `exact`, `regex`, `json-schema`, `numeric-threshold`, and advisory aggregation with `required:false`.","acceptance_criteria":"- Deleting or corrupting every case `score.json` in a copied run directory does not change `report --run-dir ... --format json` `report_hash`.\n- Tests cover `exact`, `regex`, `json-schema`, `numeric-threshold`, and advisory non-required scorers.\n- The tests drive the actual CLI wherever the contract is a CLI boundary.\n- Existing tests remain green.","notes":"COMPLETED:\n- Strengthened artifact replay test by deleting one copied score.json and corrupting the others before report --run-dir; report_hash remains stable.\n- Added CLI scorer matrix coverage for exact, regex, json-schema JSON parse, numeric-threshold, and contains as a non-required advisory scorer.\n- Verified advisory failure does not make the run/report fail when required:false.\n- Verified with python3 -m unittest discover -s tests; non-UTF-8 fixture test remains skipped on this filesystem as designed.\n\nCLOSED:\n- Implementation committed in 86af393.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:54:27.537326Z","closed_at":"2026-07-15T06:54:27.537264Z","close_reason":"Added replay regression that ignores deleted/corrupt score.json files and CLI scorer coverage for exact, regex, JSON, numeric threshold, and advisory paths — commit 86af393","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["report","tests"],"dependencies":[{"issue_id":"evalctl-qlg.10","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.10","depends_on_id":"evalctl-qlg.1","type":"blocks","created_at":"2026-07-15T06:28:41.137391Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.10","depends_on_id":"evalctl-qlg.5","type":"blocks","created_at":"2026-07-15T06:28:41.139409Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.10","depends_on_id":"evalctl-qlg.7","type":"blocks","created_at":"2026-07-15T06:28:41.141442Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.11","title":"Add release hygiene","description":"The v0.1.1 hardening batch needs release hygiene so the corrected contract is verified and documented before tagging or publishing.","design":"Add a GitHub Actions workflow that runs `python -m unittest` across supported Python versions `>=3.11` such as 3.11 through 3.13, plus the dependency-free lint check `python -m compileall evalctl tests`. Do not introduce Ruff or flake8 in this patch. Add `CHANGELOG.md` with a `0.1.1` entry listing WS1 through WS8 fixes and noting that `contract_version` remains 1. Update README language away from \"Python implementation in progress\" toward an accurate pre-release or contract-hardened posture after the batch is green. Bump version metadata to `0.1.1`.","acceptance_criteria":"- `.github/workflows/ci.yml` runs unittest and compileall across the supported Python range.\n- CI is green.\n- `CHANGELOG.md` includes a `0.1.1` entry covering WS1 through WS8 and unchanged `contract_version:1`.\n- README reflects the corrected v0.1.1 posture.\n- `pyproject.toml` and `evalctl/__init__.py` both report `0.1.1`.\n- No `0.1.1` tag or PyPI publish happens until CI, CHANGELOG, README, and version bump are complete.","notes":"COMPLETED:\n- Added .github/workflows/ci.yml with Python 3.11, 3.12, and 3.13 matrix, unittest discovery, and compileall checks.\n- Hardened workflow with workflow_dispatch, contents:read permissions, concurrency cancellation, and timeout-minutes.\n- Added CHANGELOG.md with 0.1.1 WS1-WS8 coverage and contract_version unchanged at 1.\n- Updated README status to v0.1.1 contract-hardened pre-release posture.\n- Bumped pyproject.toml and evalctl/__init__.py to 0.1.1.\n- Verified locally with python3 -m unittest discover -s tests, python3 -m compileall evalctl tests, and python3 -m evalctl --version.\n- Verified GitHub Actions CI run 29395853283 passed on Python 3.11, 3.12, and 3.13.\n\nCLOSED:\n- Release hygiene committed in 7cfebe3 and workflow hardening committed in 93687f1.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"chore","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:59:50.240599Z","closed_at":"2026-07-15T06:59:50.240543Z","close_reason":"Added v0.1.1 release hygiene: CI matrix plus compileall, CHANGELOG, README status, version bump, and verified green GitHub Actions run 29395853283 — commits 7cfebe3 and 93687f1","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["ci","release"],"dependencies":[{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.10","type":"blocks","created_at":"2026-07-15T06:28:41.160481Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.2","type":"blocks","created_at":"2026-07-15T06:28:41.145229Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.3","type":"blocks","created_at":"2026-07-15T06:28:41.151979Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.4","type":"blocks","created_at":"2026-07-15T06:28:41.143434Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.5","type":"blocks","created_at":"2026-07-15T06:28:41.150172Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.6","type":"blocks","created_at":"2026-07-15T06:28:41.154123Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.7","type":"blocks","created_at":"2026-07-15T06:28:41.156159Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.8","type":"blocks","created_at":"2026-07-15T06:28:41.158247Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.11","depends_on_id":"evalctl-qlg.9","type":"blocks","created_at":"2026-07-15T06:28:41.148007Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.2","title":"Always emit runner safety warning","description":"`W_UNSANDBOXED_RUNNER` is omitted from the JSON envelope when a suite acknowledges the unsandboxed runner. The machine contract should always expose this warning for `run`; acknowledgement should only suppress the human stderr line.","design":"Seed `all_warnings` with `W_UNSANDBOXED_RUNNER` for every `run` invocation before both the fresh-run path and the completed-run idempotent early return. Gate only the stderr advisory on `not acknowledged_unsandboxed_runner and sys.stderr.isatty()`.","acceptance_criteria":"- Running the acknowledged sample emits `W_UNSANDBOXED_RUNNER` in JSON `warnings[]` and no stderr advisory.\n- Running with acknowledgement disabled emits the same JSON warning and prints the stderr advisory only when stderr is a TTY.\n- Reusing a completed run id with `existing:true` still includes `W_UNSANDBOXED_RUNNER` in JSON `warnings[]`.","notes":"COMPLETED:\n- Seeded W_UNSANDBOXED_RUNNER for every run invocation before fresh/existing path split.\n- Preserved stderr advisory only for unacknowledged suites when stderr is a TTY.\n- Added tests for acknowledged fresh runs, completed-run reuse, and unacknowledged TTY stderr behavior.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in 30dab56.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:38:39.683440Z","closed_at":"2026-07-15T06:38:39.683397Z","close_reason":"Always emitted runner safety warning in every run envelope, including completed-run reuse, with stderr advisory gated to unacknowledged TTY runs — commit 30dab56","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["contract","run"],"dependencies":[{"issue_id":"evalctl-qlg.2","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.2","depends_on_id":"evalctl-qlg.1","type":"blocks","created_at":"2026-07-15T06:28:41.106688Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.3","title":"Make JSON artifact writes atomic","description":"Run-directory JSON artifacts are written directly. A crash or interrupted write can leave `manifest.json` or other source-of-truth artifacts truncated at the final path.","design":"Refactor `write_json` so it writes JSON text to a temporary file in the same directory and then atomically replaces the destination with `os.replace`. Apply this uniformly to manifest, runner, score, and workspace-manifest JSON writes. This guarantees atomic visibility, not full crash-durable persistence; do not add `fsync` in v0.1.1.\n\nExpose a small test seam around the low-level temp-write step so a unit test can force failure after temp creation and before replacement without brittle monkeypatching of `os`.","acceptance_criteria":"- Normal runs produce the same deterministic artifacts as before.\n- A unit test can force an interrupted write between temp-write and replace.\n- When the forced failure occurs, the previous final JSON file remains intact or absent; it is never truncated.\n- The code and docs describe the guarantee as atomic visibility, not crash durability.","notes":"COMPLETED:\n- Replaced direct JSON writes with same-directory temp file plus os.replace atomic visibility.\n- Added an injectable _atomic_write writer seam for failure tests.\n- Documented atomic visibility vs crash durability in robot docs.\n- Added a unit test proving temp-write failure preserves an existing final JSON file and does not create an absent final file.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in 97c08e5.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:40:01.573915Z","closed_at":"2026-07-15T06:40:01.573871Z","close_reason":"Made JSON artifact writes atomic via same-directory temp files and os.replace, with failure-seam tests proving final JSON remains intact — commit 97c08e5","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["artifacts","reliability"],"dependencies":[{"issue_id":"evalctl-qlg.3","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.3","depends_on_id":"evalctl-qlg.1","type":"blocks","created_at":"2026-07-15T06:28:41.108066Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.4","title":"Implement bounded jobs concurrency","description":"`--jobs` is currently recorded in the manifest but cases still execute sequentially. The CLI needs real bounded parallelism while preserving deterministic evalctl-owned outputs.","design":"Parse `--jobs` as a positive integer, defaulting to `min(os.cpu_count() or 1, 4)`, and reject values below 1 with `E_CASE_INVALID`. Run `run_case` calls through `concurrent.futures.ThreadPoolExecutor(max_workers=jobs)`. Collect `(entry, warnings)` results by case id, then rebuild `manifest.cases` and aggregated warnings in sorted case-id order.\n\nIf a runner timeout or nonzero outcome is handled inside `run_case`, keep it reportable per the runner-error contract. If a future raises unexpectedly, catch it in the orchestrator and convert it to a case error only if a minimal complete, replayable case directory can be written: `input.json`, `output.txt`, `runner.json`, `workspace-before.json`, `workspace-after.json`, `workspace-diff.json`, and `score.json`. If enough artifacts cannot be written for deterministic `report_data`, abort as a command-level exit-3 failure. If no manifest can be written, remove the empty reservation directory so the run id is not wedged as busy forever.","acceptance_criteria":"- Two 2-second sleep cases finish in about 2 seconds with `--jobs 2` and about 4 seconds with `--jobs 1`.\n- Across `--jobs 1` and `--jobs N` for deterministic runners, `report_hash`, `manifest.cases` order, per-case statuses, per-case scores, and artifact relative paths match.\n- `manifest.execution.jobs`, `runner.json.duration_ms`, and other documented volatile fields may differ.\n- `--jobs 0` and other values below 1 fail with `E_CASE_INVALID`.\n- Unexpected future failures either produce replayable errored case artifacts or abort as a command-level report-preventing failure without leaving a wedged reservation directory.","notes":"COMPLETED:\n- Added positive integer --jobs parsing with default min(os.cpu_count() or 1, 4).\n- Executed run_case calls through ThreadPoolExecutor(max_workers=jobs).\n- Aggregated case entries and warnings back in sorted case-id order.\n- Recorded effective jobs in manifest.execution.jobs.\n- Added replayable synthesized case-error artifacts for contained unexpected future failures, with command-level exit 3 and reservation cleanup when artifacts cannot be produced.\n- Added tests for two 2-second cases under --jobs 1 vs --jobs 2, normalized output determinism, invalid --jobs, and no wedged reservation on unreplayable future failure.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in 293197d.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:42:43.191714Z","closed_at":"2026-07-15T06:42:43.191662Z","close_reason":"Implemented bounded run concurrency with positive --jobs parsing, sorted deterministic aggregation, failure cleanup, and timing/determinism tests — commit 293197d","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["concurrency","run"],"dependencies":[{"issue_id":"evalctl-qlg.4","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.4","depends_on_id":"evalctl-qlg.1","type":"blocks","created_at":"2026-07-15T06:28:41.109723Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.4","depends_on_id":"evalctl-qlg.2","type":"blocks","created_at":"2026-07-15T06:28:41.111454Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.4","depends_on_id":"evalctl-qlg.3","type":"blocks","created_at":"2026-07-15T06:28:41.113385Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.5","title":"Kill runner process groups","description":"The runner is started in a new session, but the timeout branch only handles the direct child. Grandchildren can survive past the configured timeout, violating the execution contract.","design":"Replace `subprocess.run(...)` in `run_case` with explicit `subprocess.Popen(..., start_new_session=True)` and `communicate(timeout=...)`. On `TimeoutExpired`, call `os.killpg(os.getpgid(proc.pid), signal.SIGKILL)` with `ProcessLookupError` handling, then call `proc.communicate()` to drain and reap. Preserve existing capture behavior, task stdin handling, shell/argv behavior, partial stdout/stderr capture, normal negative-returncode signal reporting, and redaction.","acceptance_criteria":"- A runner that forks a long-lived grandchild and then blocks past `timeout_seconds` leaves no surviving grandchild.\n- Timeout case artifacts show `status:\"error\"`, `runner.json.timed_out:true`, and `runner.json.error_code:\"E_RUNNER_TIMEOUT\"`.\n- The run emits `W_PARTIAL_RUN`, does not emit an envelope error, exits 0 by default, and exits 6 under `--fail-on-fail`.","notes":"COMPLETED:\n- Replaced subprocess.run runner execution with explicit subprocess.Popen plus communicate(timeout=...).\n- On timeout, kills the runner process group with os.killpg(os.getpgid(proc.pid), signal.SIGKILL), handles ProcessLookupError, then drains/reaps with communicate().\n- Preserved shell/argv behavior, task stdin handling, stdout/stderr capture, timeout artifact status, and redaction flow.\n- Added a regression test where the runner spawns a child which spawns a delayed marker writer; after timeout the marker is never written.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in 22c53b8.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:44:53.702691Z","closed_at":"2026-07-15T06:44:53.702645Z","close_reason":"Killed runner process groups on timeout using Popen plus os.killpg, preserving capture semantics and adding a grandchild cleanup regression — commit 22c53b8","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["run","timeout"],"dependencies":[{"issue_id":"evalctl-qlg.5","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.5","depends_on_id":"evalctl-qlg.1","type":"blocks","created_at":"2026-07-15T06:28:41.115033Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.5","depends_on_id":"evalctl-qlg.4","type":"blocks","created_at":"2026-07-15T06:28:41.117048Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.6","title":"Enforce run-id semantic identity","description":"Reusing a completed `--run-id` against a different suite or case set currently returns the stale completed run. Agents need completed-run reuse to be idempotent only when the current suite identity matches the stored manifest.","design":"On completed run-id reuse, compare the current suite name, suite hash, and sorted list of `(case_id, input_hash)` pairs against the stored manifest. A match returns the existing run with `existing:true`. A mismatch raises `E_RUN_CONFLICT` with exit 5, names the existing run-id clash, and suggests using a fresh run id. Manifest-less reservation directories remain `E_RUN_BUSY`.\n\nEnrich the idempotent response with `run_id`, `run_dir`, `run{ok,case_count,status_counts}`, `report_hash`, and `existing:true`, using `report_data(run_dir)` where possible. Preserve `W_UNSANDBOXED_RUNNER` on this early-return path.","acceptance_criteria":"- Rerunning the same suite and same run id returns exit 0, `existing:true`, `run_id`, `run_dir`, `run`, `report_hash`, and `W_UNSANDBOXED_RUNNER`.\n- Editing a case body and reusing the same run id returns `E_RUN_CONFLICT`, exit 5, and does not overwrite artifacts.\n- Renaming a case id with identical content also returns `E_RUN_CONFLICT`, exit 5, and does not overwrite artifacts.\n- A manifest-less reservation directory still returns `E_RUN_BUSY`, exit 4.","notes":"COMPLETED:\n- Added current/stored run identity comparison using suite name, suite hash, and sorted (case_id, input_hash) pairs.\n- Completed-run reuse now returns existing:true with run_id, run_dir, run summary, report_hash, and W_UNSANDBOXED_RUNNER.\n- Reuse with edited case content or renamed case id now raises E_RUN_CONFLICT exit 5 without overwriting artifacts.\n- Manifest-less reservation directories still return E_RUN_BUSY exit 4.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in 178000b.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:47:45.308199Z","closed_at":"2026-07-15T06:47:45.308149Z","close_reason":"Enforced run-id semantic identity using suite hash and sorted case id/input hashes, enriched idempotent responses, and preserved busy/conflict behavior — commit 178000b","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["idempotency","run"],"dependencies":[{"issue_id":"evalctl-qlg.6","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.6","depends_on_id":"evalctl-qlg.2","type":"blocks","created_at":"2026-07-15T06:28:41.118944Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.6","depends_on_id":"evalctl-qlg.4","type":"blocks","created_at":"2026-07-15T06:28:41.121388Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.7","title":"Cap output file bytes","description":"`EVALCTL_OUTPUT_FILE` is read without a cap and `runner.json.output_truncated` is always false. This can produce unbounded artifacts and dishonest metadata.","design":"Read at most `max_output_bytes` raw bytes from `EVALCTL_OUTPUT_FILE`, then decode with UTF-8 replacement, mirroring stdout/stderr handling. Set `runner.json.output_truncated` truthfully and include output-file truncation in `W_OUTPUT_TRUNCATED`. Truncate before redaction and before writing back to `output.txt` so the persisted artifact and scored text match. Keep scorers text-based on the decoded replacement text; byte-exact scoring is out of scope.","acceptance_criteria":"- A runner that writes an ASCII output file larger than `max_output_bytes` produces `runner.json.output_truncated:true`.\n- The run emits `W_OUTPUT_TRUNCATED`.\n- The captured raw bytes are capped at `max_output_bytes`; the persisted decoded file may differ in on-disk byte length for invalid or multibyte input.\n- Scoring and report replay use the capped persisted artifact.","notes":"COMPLETED:\n- Capped EVALCTL_OUTPUT_FILE raw bytes at max_output_bytes before UTF-8 replacement decode and redaction.\n- Persisted the capped decoded output.txt so scorers and artifact replay use the same text.\n- Set runner.json.output_truncated truthfully and emit W_OUTPUT_TRUNCATED when stdout, stderr, or output file capture is capped.\n- Added a CLI regression where oversized ASCII output is capped to 10 bytes, exact-scored, and report replay preserves the report_hash.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in ef862a2.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:50:15.299981Z","closed_at":"2026-07-15T06:50:15.299925Z","close_reason":"Capped EVALCTL_OUTPUT_FILE raw bytes before decode/redaction, set output_truncated, emitted W_OUTPUT_TRUNCATED, and tested replay from capped artifacts — commit ef862a2","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["artifacts","run"],"dependencies":[{"issue_id":"evalctl-qlg.7","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.7","depends_on_id":"evalctl-qlg.4","type":"blocks","created_at":"2026-07-15T06:28:41.123755Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.8","title":"Skip non-UTF-8 paths","description":"Workspace paths containing surrogate-escaped non-UTF-8 bytes can survive the manifest walk and later crash JSON serialization. The manifest contract should skip unreadable or unrepresentable paths with a warning rather than crashing the run.","design":"In manifest path normalization, detect relative paths that cannot round-trip through strict UTF-8. Explicitly reject surrogate code points before any `stable_json(..., ensure_ascii=False)` serialization. Skip such entries and emit `W_PATH_UNREADABLE` with a best-effort lossy path. Preserve existing symlink, FIFO, and hash-error handling.","acceptance_criteria":"- A POSIX-only, gracefully skipped test creates a bad-byte fixture through raw bytes where the platform/filesystem allows it.\n- The manifest omits the non-UTF-8 path.\n- The run emits `W_PATH_UNREADABLE` naming the problematic path.\n- The run still completes and scores other paths.","notes":"COMPLETED:\n- Changed manifest traversal sorting to use filesystem bytes so a bad relative path cannot crash before the per-path try block.\n- normalize_rel now rejects paths that cannot encode as strict UTF-8, including surrogateescaped byte names.\n- W_PATH_UNREADABLE messages use lossy-safe display names so warnings remain JSON-serializable.\n- Added a POSIX-only/skippable raw-byte fixture test that asserts warning emission, manifest omission, and run completion where the filesystem permits the fixture.\n- Verified with python3 -m unittest discover -s tests; the raw-byte fixture test skipped on this UTF-8-enforcing filesystem as designed.\n\nCLOSED:\n- Implementation committed in 8205713.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:52:26.524061Z","closed_at":"2026-07-15T06:52:26.524019Z","close_reason":"Skipped non-UTF-8 manifest paths by rejecting surrogateescaped names, warning with lossy-safe display paths, and adding POSIX/skippable raw-byte coverage — commit 8205713","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["manifest","portability"],"dependencies":[{"issue_id":"evalctl-qlg.8","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.8","depends_on_id":"evalctl-qlg.4","type":"blocks","created_at":"2026-07-15T06:28:41.125784Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-qlg.9","title":"Ship real schema output","description":"`schema <verb>` currently returns generic object schemas with empty definitions. That creates false confidence for agents relying on the advertised machine contract.","design":"Keep the existing envelope schema and add a real module-level schema map for each implemented verb's `data` payload: `capabilities`, `schema`, `init`, `validate`, `run`, `status`, and `report`. Each schema should declare `type`, `properties`, and `required`. Top-level `data` payload schemas remain open with `additionalProperties:true` so additive fields under `contract_version:1` do not break strict validators. Close only fixed leaf structures explicitly intended never to grow.\n\nInclude current fields such as the `surface` discriminator in capabilities error codes and `existing` on the idempotent `run` response. Real schemas may change the `schema` and `capabilities` golden hashes; re-pin them intentionally.","acceptance_criteria":"- `schema run --json` returns a non-trivial `data.schemas.run` with `properties`, `required`, and top-level `additionalProperties:true`.\n- All implemented verbs have genuine `data` payload schemas.\n- Schema definitions are single-sourced and testable.\n- The `schema` golden hash is re-pinned.\n- The `capabilities` golden hash is re-pinned if its payload changed.","notes":"COMPLETED:\n- Added a module-level DATA_SCHEMAS map for capabilities, schema, init, validate, run, status, and report data payloads.\n- Each implemented verb schema now has properties, required, and top-level additionalProperties:true.\n- Included current fields such as error-code surface metadata and idempotent run existing flag.\n- Re-pinned the schema run data-hash golden.\n- Added tests proving every implemented verb schema is non-trivial and open at the top-level payload.\n- Verified with python3 -m unittest discover -s tests.\n\nCLOSED:\n- Implementation committed in 37fa250.\n\nBLOCKERS:\n- None.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T06:28:41.051966Z","updated_at":"2026-07-15T06:56:58.111007Z","closed_at":"2026-07-15T06:56:58.110942Z","close_reason":"Shipped real per-verb schema payloads with open top-level data schemas, current run/capabilities fields, and re-pinned schema golden tests — commit 37fa250","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["contract","schema"],"dependencies":[{"issue_id":"evalctl-qlg.9","depends_on_id":"evalctl-qlg","type":"parent-child","created_at":"2026-07-15T06:28:41.051966Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.9","depends_on_id":"evalctl-qlg.1","type":"blocks","created_at":"2026-07-15T06:28:41.127432Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.9","depends_on_id":"evalctl-qlg.5","type":"blocks","created_at":"2026-07-15T06:28:41.129553Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.9","depends_on_id":"evalctl-qlg.6","type":"blocks","created_at":"2026-07-15T06:28:41.131709Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.9","depends_on_id":"evalctl-qlg.7","type":"blocks","created_at":"2026-07-15T06:28:41.133741Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-qlg.9","depends_on_id":"evalctl-qlg.8","type":"blocks","created_at":"2026-07-15T06:28:41.135770Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b","title":"Evalctl v0.2 Authoring and Execution Replay","description":"The v0.1 CLI can run and report on suites, but suites can only be built by hand-editing\nJSON, and only artifact replay exists — there is no execution replay to re-run a failed\ncase after a fix. v0.2 makes evalctl authorable via CLI verbs (`suite add`, `case add`,\n`scorer add`), adds `replay --failed` execution replay, and opens the external\ncommand-scorer seam. Every addition is additive: new verbs and a new scorer type, no\nchange to the universal envelope shape.","acceptance_criteria":"- No hand-editing of JSON is required to build and run a suite (end-to-end authoring loop).\n- `replay --failed` re-runs a red case green after a fix, in a fresh run whose manifest `replayed_from` names the source.\n- A command-scorer's verdict survives artifact replay: a copied run dir with the scorer binary removed still reproduces `report_hash`.\n- Deterministic-scorer suites' `report_hash` is unchanged from v0.1.1.\n- Version is `0.2.0`; `contract_version` remains 1; `schema` and `capabilities` goldens re-pinned.\n- Every child issue's acceptance check is complete; CI green.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:05.838051Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","agent_context":"{\"constraints\":[\"Keep contract_version at 1; v0.2 is additive (new verbs and a command scorer type), no universal envelope shape change.\",\"Command-scorer verdicts are captured per-case artifacts (cases/<id>/scorers/<id>.json), written once at run time and READ, never re-executed, at report/replay time; deterministic scorers keep recomputing from artifacts. This preserves the artifact-replay guarantee.\",\"Per-case scorer failures (nonzero exit, timeout, unparseable or invalid verdict) are case-level facts: status error, W_PARTIAL_RUN, run exits 0 (6 under --fail-on-fail), recorded via error_code E_SCORER_CASE_FAILED with surface score_json. Never envelope errors[].\",\"Replay produces a coherent PARTIAL run over exactly the re-executed cases, snapshotting the CURRENT suite; manifest.suite.case_count is the replay-subset count; manifest.replayed_from names the source. Never mix two suite snapshots in one run dir.\",\"Replay selects failed targets from report_data(source) (recomputed projection), never from stored manifest.cases[].status.\",\"Authoring verbs are idempotent on the canonical on-disk object, gated with E_RUN_CONFLICT for same-key/different-content, and never generate paths that escape the suite tree (reject absolute or .. workspace/diff paths and unsafe scorer ids).\",\"Test CLI contract behavior by driving the actual CLI and asserting the JSON envelope; keep the project zero-dependency (compileall, no linter dependency).\"],\"preferences\":[\"Land tests with each workstream rather than batching them at the end.\",\"Keep data-payload schemas open at the top level; re-pin the schema and capabilities goldens once, after all payload shapes settle.\",\"Carry the command-scorer id through every scores projection (result object, manifest case entry, report/status failures); omit the key when absent so deterministic report_hash stays byte-stable.\",\"Reuse the existing run-path executor for replay by factoring a shared execute_cases helper out of command_run.\"],\"context\":[\"v0.2 makes evalctl authorable without hand-editing JSON and closes the execution-replay half of the replay promise; the wedge stays: unit under test = agent run, scoring surface = resulting workspace.\",\"The command-scorer protocol is the future seam for an LLM-judge scorer, kept deterministic-first; no judge scorer ships in v0.2.\"]}","compaction_level":0,"original_size":0,"labels":["authoring","release","replay","v0-2"]}
{"id":"evalctl-t5b.1","title":"Add command-scorer protocol","description":"`run_scorer` is a closed dispatch over eight deterministic built-in scorers; there is no\nway to score a case with an external program. v0.2 adds a `command` scorer type — the\nexternal scorer seam that is also the future LLM-judge seam — so a suite can score a case\nby running an arbitrary program that emits a JSON verdict. Because report and replay\nrecompute case status from artifacts and never trust `score.json`, a non-deterministic\nexternal scorer must be captured as an artifact, not re-executed.","design":"Add a `command` branch to `run_scorer`, and thread `case_dir: Path` and `execute: bool`\nthrough `score_case` and `run_scorer`. `run_case` calls with `execute=True`;\n`recompute_case_score` calls with `execute=False`.\n\nScorer config entry:\n`{\"name\":\"command\",\"id\":\"<unique>\",\"required\":bool,\"argv\":[...]|\"command\":\"...\",\"shell\":bool,\"timeout_seconds\":N}`.\nThe `id` is required for command scorers (a suite may have several, all named\n`\"command\"`), must be path-safe (no separators, no `..`, no unsafe filename chars), and\nnames the verdict artifact.\n\nExecute path (run time): invoke once per case with `EVALCTL_CASE_FILE` (persisted\n`input.json`), `EVALCTL_OUTPUT_FILE` (persisted `output.txt`), and `EVALCTL_WORKSPACE`;\ncwd is the case workspace; argv/command gets the same `$EVALCTL_*` substitution as the\nrunner. Persist the parsed verdict to `cases/<case_id>/scorers/<id>.json`. This artifact\nis a runner-class input, captured once, never regenerated.\n\nReplay path (report time): read the persisted verdict artifact; never execute. Deterministic\nscorers keep recomputing as today.\n\nProcess hardening (mirror the runner): `subprocess.Popen(..., start_new_session=True)`\nwith `os.killpg` process-group kill on timeout; `timeout_seconds` from a named default\nwhen omitted; byte-capped stdout/stderr with decode-and-replace; apply the suite's\nredaction patterns and redacted env values before persisting; write capped/redacted\n`cases/<case_id>/scorers/<id>.stdout.txt` and `.stderr.txt` for debugging.\n\nVerdict normalization: stdout must parse as exactly one JSON object after trimming\nwhitespace (extra non-whitespace output is a scorer error); `ok` required boolean;\n`score` required finite numeric constrained to 0.0..1.0 (reject NaN/Infinity and\nout-of-range); `label` required string, normalized to `\"pass\"`/`\"fail\"` from `ok` when\nmissing/empty; `findings` required list, string entries normalized to `{\"why\": <string>}`.\n`error:true` from a zero-exit process is a case error.\n\nStatus mapping: required scorer `ok:false,error:false` -> case `fail`; advisory scorer\n`ok:false` does not fail the case; `error:true` or any normalization failure -> case\n`error` regardless of `required`. A nonzero exit, timeout, or unparseable/invalid verdict\nproduces a scorer result carrying `error:true` and `error_code:\"E_SCORER_CASE_FAILED\"`,\nwhich drives the case to `status:\"error\"` via the existing `s.get(\"error\")` path — never\nan orchestrator abort.\n\nCarry the scorer `id` through every scores projection: the result object, the manifest\ncase entry, and the report/status failure projections — omitting the key when absent so\ndeterministic-only suites' `report_hash` is byte-unchanged.\n\nAdd `E_SCORER_CASE_FAILED` to the code registry (class `tool-env`, surface `score_json`\nas a machine-token parallel to `runner_json`, where `[\"run\",\"replay\"]`); keep the\nexisting envelope-abort `E_SCORER_FAILED` distinct. A command-scorer is arbitrary local\ncode covered by the existing always-in-envelope unsandboxed-runner warning.","acceptance_criteria":"- A suite with a passing command-scorer scores the case pass and writes `cases/<id>/scorers/<id>.json`.\n- Copy the run dir, delete the scorer binary, and `report --run-dir <copy>` reproduces the original `report_hash` (verdict is an artifact, never re-executed).\n- Two command scorers, one passing and one failing: the report identifies the failing scorer by `id`.\n- Invalid `score` type, NaN/Infinity, missing `ok`, extra stdout, and `error:true` from a zero-exit process each yield case `error`; a required scorer `ok:false,error:false` yields `fail`; an advisory scorer `ok:false` does not fail the case.\n- A nonzero-exit or non-JSON scorer yields `status:\"error\"`, `W_PARTIAL_RUN`, run exits 0 (6 under `--fail-on-fail`), no envelope `errors[]`.\n- Command-scorer timeout kills the process group; stdout/stderr are byte-capped and redacted in the persisted debug artifacts.","notes":"COMPLETED:\n- Implemented command-scorer execution and report-time artifact replay.\n- Added verdict normalization, per-case scorer failure handling, scorer id projection, timeout process-group kill, redacted/capped debug artifacts.\n- Added CLI-surface tests for command-scorer pass/fail/error/advisory/timeout behavior.\n\nIN_PROGRESS: none.\nNEXT: commit implementation, close bead, then continue to replay --failed.\nBLOCKERS:\n- Full test suite has one expected capabilities data_hash failure until the schema/capabilities bead re-pins goldens.","status":"closed","priority":1,"issue_type":"task","assignee":"dave","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T18:50:23.625230Z","closed_at":"2026-07-15T18:50:23.625190Z","close_reason":"Added command-scorer protocol with captured verdict artifacts, replay-safe report recomputation, scorer id projections, process hardening, and CLI tests — commit 4699ddc","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["command-scorer","run","scorer"],"dependencies":[{"issue_id":"evalctl-t5b.1","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b.2","title":"Implement replay --failed execution replay","description":"Only artifact replay exists today: a copied run dir reproduces its report, but there is\nno way to re-execute a failed case after fixing the runner or fixtures. `replay --failed`\nre-runs only the failed/errored cases of a prior run against the current suite, into a\nfresh run linked to the source, so a fix turns a red case green in a new run. `manifest.replayed_from`\nis already reserved but never populated.","design":"Add `evalctl replay --failed <source-run-id|--run-dir PATH> [--suite S] [--run-id NEW] [--force] [--json]`\nwith its own source parser — do NOT reuse `resolve_run`, which strips only\n`--run-dir`/`--format` and would misread `--failed`/`--run-id`. Exactly one source is\nrequired (positional id XOR `--run-dir`; both or neither -> `E_CASE_INVALID`). `--run-id`\nis destination-only. `--failed` is the only selector in v0.2; missing it -> `E_CASE_INVALID`.\n\nSelect failed targets from `report_data(source_run)` (the recomputed projection), NOT\nfrom stored `manifest.cases[].status` — the manifest status is a run-time summary, while\nthe authoritative reportable state is recomputed from artifacts (command-scorer verdicts\nincluded). Targets are cases whose recomputed `status != \"pass\"`. Use the manifest only\nfor provenance and id/artifact metadata.\n\nResolve the current suite: default to `manifest.suite.name` resolved the way `run`\nresolves a suite; `--suite <suite-or-path>` overrides (needed because the manifest stores\nonly suite name+hash, `suite.json.name` can differ from the directory, and a copied\n`--run-dir` may sit where `evals/suites/<name>` does not exist). Resolution failure ->\n`E_SUITE_NOT_FOUND` with a hint to pass `--suite`. Intersect targets with the current\nsuite's cases; a selected case absent from the current suite is dropped with\n`W_REPLAY_CASE_ABSENT`, never fabricated.\n\nProduce a coherent PARTIAL run over exactly the re-executed cases: fresh run dir (default\nrun id `<source>-replay-<UTC-ts>`, `--run-id` overrides), snapshot the CURRENT suite,\nre-run the selected cases through the shared executor. Factor the executor loop out of\n`command_run` into a shared `execute_cases(...)` helper both call. Write the manifest with\n`replayed_from = <source run_id>` and `cases[]` = the replayed subset; `manifest.suite.case_count`\nis the subset count, explicitly not the full current suite count. Emit the always-in-envelope\nunsandboxed-runner warning (replay re-executes runner code) and `W_PARTIAL_RUN` when any\nreplayed case errors.\n\nOverwrite gating: fresh dir needs no gate. `--run-id` naming an existing completed run\nrequires `--force`; without it -> `E_RUN_CONFLICT` (exit 5). `--force` does not remove a\nbusy reservation dir (stays `E_RUN_BUSY`). Reject destination == source even under\n`--force` (e.g. `--run-id r1` on source `r1`, or a `--run-dir` destination resolving to\nthe source path) with `E_RUN_CONFLICT` before writing anything, so replay never destroys\nthe source it needs.\n\nZero targets (all-green source) is a successful no-op: exit 0 with\n`{\"replayed_from\":...,\"cases_replayed\":0}` and `W_NOTHING_TO_REPLAY`.\n\nAdd `W_REPLAY_CASE_ABSENT` and `W_NOTHING_TO_REPLAY` to the registry; add `replay` to the\n`where` of `E_RUN_CONFLICT` and the unsandboxed-runner warning.","acceptance_criteria":"- A red case + a runner fix, then `replay --failed <run>`, yields a fresh run whose `manifest.replayed_from` is the source id, containing only the previously-red case, now `status:\"pass\"`, exit 0.\n- Deleting/corrupting the source `cases/*/score.json` still lets `report <source>` name the red case AND `replay --failed` select the same case (selection from `report_data`, not the manifest summary).\n- An all-green source yields `cases_replayed:0`, `W_NOTHING_TO_REPLAY`, exit 0.\n- Source id both before and after replay flags resolves to the same source; `--run-dir PATH --run-id NEW` keeps source and destination distinct; missing `--failed` -> `E_CASE_INVALID`; both positional source and `--run-dir` -> `E_CASE_INVALID`.\n- A suite whose `suite.json.name` differs from its directory, run by path, then replayed with `--suite <path>` after a fix, resolves and re-runs correctly.\n- `--run-id` onto an existing completed run without `--force` -> `E_RUN_CONFLICT` exit 5; with `--force` overwrite succeeds; `replay --failed r1 --run-id r1 --force` -> `E_RUN_CONFLICT` exit 5 with the source manifest byte-identical.\n- A selected case removed from the current suite -> `W_REPLAY_CASE_ABSENT`, skipped.","status":"open","priority":1,"issue_type":"task","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:00.206318Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["replay","run"],"dependencies":[{"issue_id":"evalctl-t5b.2","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.2","depends_on_id":"evalctl-t5b.1","type":"blocks","created_at":"2026-07-15T17:52:00.206234Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b.3","title":"Add suite add authoring verb","description":"Suites can only be created by `init_project`, which scaffolds the fixed sample. There is\nno way to create a new empty suite from the CLI, forcing hand-authored JSON. `suite add`\ncreates a minimal valid suite that `case add` and `scorer add` then fill.","design":"Add `evalctl suite add <name> [--runner-argv \"...\"] [--runner-command \"...\"] [--shell] [--json]`.\nCreate `evals/suites/<name>/` with a minimal valid `suite.json` (name,\n`cases: \"cases.jsonl\"`, a runner block from the flags with the v0.1 defaults for\n`timeout_seconds`, `max_output_bytes`, `env_allowlist`, empty redaction), an empty\n`cases.jsonl`, and a `fixtures/` dir. Runner form is mutually exclusive (`--runner-argv`\nXOR `--runner-command`+`--shell`), validated up front the way `validate_suite` validates\nrunner config.\n\nThe `<name>` is path-contained: reject path separators, `..`, empty segments, or any name\nthat normalizes outside `evals/suites` with `E_CASE_INVALID`.\n\nAtomic multi-file creation: a new suite is a directory plus multiple files, so per-file\natomic writes do not prevent a half-created suite dir. If the destination already exists,\ndecide idempotent-match vs conflict before writing anything. Otherwise build a temporary\nsibling dir under `evals/suites/`, populate it fully, run `validate_suite` on it, then\n`os.replace`/rename into place only on success.\n\nIdempotency: an existing suite dir with a byte-identical `suite.json` -> `created:false`,\nreturn name. Existing with different content -> `E_RUN_CONFLICT` (exit 5). Never overwrite\nan authored suite. An empty suite (zero cases) validates as `case_count:0, valid:true`.","acceptance_criteria":"- `suite add demo --runner-argv \"python3 $EVALCTL_WORKSPACE/r.py\"` creates `evals/suites/demo/{suite.json,cases.jsonl,fixtures/}`, and `validate demo` passes with `case_count:0`.\n- Re-running the identical command -> `created:false`.\n- The same name with a different runner -> `E_RUN_CONFLICT`, exit 5.\n- A name with a path separator or `..` -> `E_CASE_INVALID`.\n- A forced failure mid-creation leaves no half-built suite dir (temp-dir-and-rename).","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:00.154811Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["authoring","suite"],"dependencies":[{"issue_id":"evalctl-t5b.3","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b.4","title":"Add case add authoring verb","description":"Cases exist only as hand-written JSONL lines; no verb appends one. `case add` appends a\nvalid case to an existing suite, assigning and returning its id, so a suite can be built\nentirely from the CLI.","design":"Add `evalctl case add <suite> --task \"...\" --workspace <path> [--id ID] [--diff <path>] [--expect-json '<json>'] [--json]`.\nAppend one line to `cases.jsonl` in the existing on-disk form (one `stable_json(case)`\nper line, trailing newline) so unrelated cases do not churn.\n\nCanonical construction order (this is how idempotency is judged): parse flags -> build the\nbody WITHOUT id -> compute the generated id if `--id` is absent (the same rule\n`load_cases` uses: content-hash slice) -> set `id` -> serialize with `stable_json`. Retry\ncomparison is against that canonical body including the generated id, so a retry never\nlooks different merely because one object had an explicit id and the other did not.\n\n`--expect-json` parses into the case's `expect` field, not a new `expect_json` field; an\nabsent/empty `--expect-json` omits `expect` entirely (does not write `\"expect\": {}`) and\nretries must match that choice.\n\nPath containment: reject absolute `--workspace`/`--diff` and any relative path containing\n`..` with `E_CASE_INVALID`; store normalized POSIX-relative paths. The workspace (and diff\nif given) must also exist under the suite dir, matching the validator.\n\nIdempotency: an existing case whose canonical body matches -> `created:false`, return id.\nSame id, different canonical body -> `E_RUN_CONFLICT` (exit 5). Reuse the existing\nduplicate-id guard. Run `validate_suite` after the write; on failure roll back the\nappended line by restoring the prior file bytes.","acceptance_criteria":"- `case add demo --task \"do X\" --workspace fixtures/x` (fixture created beforehand) appends one valid line, returns the id, and `validate demo` reports `case_count` incremented.\n- Running the same command again without `--id` -> `created:false` and `cases.jsonl` byte-identical (canonical-body comparison).\n- Same id, changed task -> `E_RUN_CONFLICT`, exit 5.\n- `--workspace ../x`, an absolute `--workspace`, or a nonexistent `--workspace` -> `E_CASE_INVALID` exit 1, no line written.\n- Unrelated cases' lines are byte-unchanged (no hash churn).","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:00.207683Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["authoring","case"],"dependencies":[{"issue_id":"evalctl-t5b.4","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.4","depends_on_id":"evalctl-t5b.3","type":"blocks","created_at":"2026-07-15T17:52:00.207610Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b.5","title":"Add scorer add authoring verb","description":"Scorers can only be configured by hand-editing the `scorers` array in `suite.json`.\n`scorer add` appends a scorer entry (built-in deterministic or a command scorer),\ncompleting the CLI-only authoring loop.","design":"Add `evalctl scorer add <suite> --name <scorer> [--required|--advisory] [--id ID] [--command \"...\"|--argv \"...\"] [--shell] [--timeout N] [--json]`.\nAppend to `suite.json`'s `scorers[]`, atomically rewriting `suite.json` preserving the\nexisting `indent=2, sort_keys=True` form so unrelated keys do not churn.\n\nValidate `--name` against the known deterministic set plus `command`; unknown ->\n`E_CASE_INVALID`. For `--name command`, a unique path-safe `--id` and a runner form are\nrequired (the id names the per-case verdict artifact consumed by the command-scorer path).\n\nUniqueness keys: a deterministic scorer is keyed by `name` (built-ins carry no id — a\nsecond entry with the same name and different config is a conflict, not a legal\ndouble-count that would skew the aggregate); a command scorer is keyed by `id`.\nByte-identical entry already present -> `created:false`. Matching key with different\nconfig -> `E_RUN_CONFLICT` (exit 5). A new command-scorer id colliding with an existing\none -> `E_RUN_CONFLICT`. An `--id` with path separators or unsafe filename chars ->\n`E_CASE_INVALID`. Run `validate_suite` after the write; roll back on failure.","acceptance_criteria":"- `scorer add demo --name exact --required` appends the entry and `validate` still passes.\n- `scorer add demo --name command --id judge1 --argv \"...\"` appends a command-scorer entry usable by the command-scorer path.\n- Re-adding an identical entry -> `created:false`.\n- `scorer add demo --name contains --required` then `scorer add demo --name contains --advisory` -> `E_RUN_CONFLICT` exit 5, `suite.json` unchanged (built-in key is `name`).\n- A conflicting command `--id`, an unknown scorer name, or an `--id` containing `/` behaves as specified (`E_RUN_CONFLICT` for id-config clash; `E_CASE_INVALID` for unknown name or unsafe id).","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:00.211105Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["authoring","scorer"],"dependencies":[{"issue_id":"evalctl-t5b.5","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.5","depends_on_id":"evalctl-t5b.1","type":"related","created_at":"2026-07-15T17:52:00.210992Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.5","depends_on_id":"evalctl-t5b.3","type":"blocks","created_at":"2026-07-15T17:52:00.209307Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b.6","title":"Fold in v0.2 schemas and capabilities","description":"The machine contract must advertise the new verbs, schemas, and codes so agents can\ndiscover them. This is the mechanical fold-in, done last so payload shapes are settled and\nthe goldens are re-pinned exactly once.","design":"Add `DATA_SCHEMAS` entries for `replay`, `suite`, `case`, `scorer` (open top-level, typed\n`properties`, `required`, following the v0.1.1 schema shape), and matching `verbs{}`\nentries in `capabilities_data` describing args/flags/`mutates`/`exit_codes`.\n\nRegistry edits, precise: add `E_SCORER_CASE_FAILED` (class `tool-env`, surface\n`score_json`, where `[\"run\",\"replay\"]`); add `W_REPLAY_CASE_ABSENT` and\n`W_NOTHING_TO_REPLAY` (warnings, where `[\"replay\"]`, surface `envelope`); grow\n`E_RUN_CONFLICT.where` from `[\"run\",\"init\"]` to include `replay`, `suite`, `case`,\n`scorer`; grow the unsandboxed-runner warning's `where` to include `replay`.\n\nExtend `robot_docs` with an authoring + replay + command-scorer section, including the\nper-case-vs-envelope surface distinction (`score_json` token parallel to `runner_json`).\nUpdate `help_text` COMMANDS, including the note that `replay --run-id` is\ndestination-only. Re-pin the `schema` and `capabilities` data-hash goldens.","acceptance_criteria":"- `schema replay --json`, `schema suite --json`, `schema case --json`, `schema scorer --json` each return non-trivial per-verb `properties` and `required` with top-level `additionalProperties: true`.\n- `capabilities --json` lists the four new verbs and the new codes with correct `surface` and `where`.\n- The `schema` and `capabilities` data-hash goldens are re-pinned; no other golden moves.","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:00.220123Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["capabilities","docs","schema"],"dependencies":[{"issue_id":"evalctl-t5b.6","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.6","depends_on_id":"evalctl-t5b.1","type":"blocks","created_at":"2026-07-15T17:52:00.213010Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.6","depends_on_id":"evalctl-t5b.2","type":"blocks","created_at":"2026-07-15T17:52:00.214862Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.6","depends_on_id":"evalctl-t5b.3","type":"blocks","created_at":"2026-07-15T17:52:00.216544Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.6","depends_on_id":"evalctl-t5b.4","type":"blocks","created_at":"2026-07-15T17:52:00.218296Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.6","depends_on_id":"evalctl-t5b.5","type":"blocks","created_at":"2026-07-15T17:52:00.220050Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b.7","title":"Add v0.2 cross-workstream regressions","description":"Per-workstream tests land with each workstream, but the cross-cutting guarantees span\nmultiple verbs and must be pinned as one suite: the end-to-end authoring loop, the\nartifact-replay guarantee under command scorers, and the promise that existing\ndeterministic suites are byte-for-byte unchanged.","design":"Add CLI-surface tests (drive the real CLI, assert the JSON envelope):\n- End-to-end authoring loop: `suite add` -> create a fixture -> `case add` -> `scorer add`\n  -> `run` -> assert a real report, with no hand-edited JSON at any step.\n- Regression guard: the full v0.1.1 test suite stays green, and the sample deterministic\n  suite's `report_hash` is byte-unchanged from v0.1.1.\nThese complement, not replace, the per-workstream tests owned by each feature issue.","acceptance_criteria":"- The end-to-end authoring-loop test passes with zero hand-edited JSON.\n- The v0.1.1 test suite passes unchanged.\n- The sample suite's `report_hash` matches its v0.1.1 value.","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:00.225413Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["regression","testing"],"dependencies":[{"issue_id":"evalctl-t5b.7","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.7","depends_on_id":"evalctl-t5b.2","type":"blocks","created_at":"2026-07-15T17:52:00.221834Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.7","depends_on_id":"evalctl-t5b.4","type":"blocks","created_at":"2026-07-15T17:52:00.223598Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.7","depends_on_id":"evalctl-t5b.5","type":"blocks","created_at":"2026-07-15T17:52:00.225343Z","created_by":"dave","metadata":"{}","thread_id":""}]}
{"id":"evalctl-t5b.8","title":"Add v0.2 release hygiene","description":"Ship the release cleanly: bump the version, document the new surface, and confirm CI is\ngreen before tagging. The publish decision (repo visibility, package index) is a separate\noutward action gated on explicit greenlight and is out of scope here.","design":"Bump the version to `0.2.0` in `pyproject.toml` and `evalctl/__init__.py`. Add a `0.2.0`\nCHANGELOG entry listing the authoring verbs, execution replay, and command-scorer protocol\nas features, and stating `contract_version` unchanged (1) with capabilities and schema\nhashes re-pinned (additive). Update the README with the authoring + replay + command-scorer\nsections and the new verb list. The existing CI workflow (unittest discovery + compileall\nacross the supported Python range) stays; add no linter dependency. Gate the tag on green CI.","acceptance_criteria":"- Version reads `0.2.0` in `pyproject.toml` and `evalctl/__init__.py`.\n- CHANGELOG has a `0.2.0` entry covering the new features and the contract note.\n- README documents the authoring verbs, replay, and command-scorer protocol.\n- CI passes on the release commit; `contract_version` is still 1.","status":"open","priority":2,"issue_type":"chore","created_at":"2026-07-15T17:52:00.154811Z","updated_at":"2026-07-15T17:52:00.230010Z","source_repo":"evalctl","source_repo_path":"/Users/dave/p/evalctl/evalctl","compaction_level":0,"original_size":0,"labels":["release"],"dependencies":[{"issue_id":"evalctl-t5b.8","depends_on_id":"evalctl-t5b","type":"parent-child","created_at":"2026-07-15T17:52:00.154811Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.8","depends_on_id":"evalctl-t5b.6","type":"blocks","created_at":"2026-07-15T17:52:00.227655Z","created_by":"dave","metadata":"{}","thread_id":""},{"issue_id":"evalctl-t5b.8","depends_on_id":"evalctl-t5b.7","type":"blocks","created_at":"2026-07-15T17:52:00.229940Z","created_by":"dave","metadata":"{}","thread_id":""}]}
